server:
  port: 8080

spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: "jdbc:postgresql://localhost:5432/uni-tech"
    username: postgres
    password: admin

  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: update
    show-sql: true